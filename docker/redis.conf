# Redis configuration for tool-chat

# Set a password if desired (leave blank for no password)
# requirepass yourpassword

# Enable append-only file persistence
appendonly yes

# Uncomment and set the correct path if you want RedisBloom
# loadmodule /usr/local/lib/redis/modules/redisbloom.so
